Top Carriers leaderboard row. Sits inside a `<Card flush>` with a header row of column labels above.

```jsx
<CarrierRow rank={1} name="Arkas Logistics" location="Belfast, United Kingdom"
  rating="8.7" reviews={116} vehicles={21} partners={12} />
```
