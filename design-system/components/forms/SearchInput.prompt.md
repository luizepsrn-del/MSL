Leading magnifier, optional keyboard-hint chip. The sidebar and every table toolbar use it.

```jsx
<SearchInput placeholder="Search" shortcut="F" fullWidth />
<SearchInput placeholder="Search or type command" fullWidth />
```

Focus swaps the hairline to `--border-focus`; no glow.
