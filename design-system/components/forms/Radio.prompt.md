Inline single-choice row; used for chart series selection, not for forms.

```jsx
<Radio value={series} onChange={setSeries}
  options={[{ value: 'last', label: 'Last Year' }, { value: 'prev', label: 'Previous Year' }]} />
```
