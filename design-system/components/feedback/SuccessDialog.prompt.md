Confirmation dialog with a glowing purple tick. This is how the product acknowledges a completed action — it has no toast pattern.

```jsx
<SuccessDialog open={done} onClose={dismiss} title="Order updated!"
  message="Your changes have been successfully applied" />
```
