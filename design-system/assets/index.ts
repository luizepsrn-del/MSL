/**
 * The system's two images, the only ones in the source material.
 * Imported through Vite so they are hashed and bundled — never referenced by
 * a hand-written path from application code.
 *
 * See DESIGN.md → Backgrounds and imagery for how they may be used.
 */
import promoRocket from './img/promo-rocket.png';
import promoLogisticsCollage from './img/promo-logistics-collage.png';

export { promoRocket, promoLogisticsCollage };
